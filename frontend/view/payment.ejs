<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Payment</title>
  <link rel="stylesheet" href="/css/payment.css" />
  <style>
    .input-wrapper {
      position: relative;
      display: inline-block;
      width: 100%;
      max-width: 500px;
    }

    #amount {
      width: 100%;
      padding-right: 30px; /* space for the icon */
      box-sizing: border-box;
    }

    .tooltip-icon {
      position: absolute;
      right: 8px;
      top: 50%;
      transform: translateY(-50%);
      background-color: transparent;
      border-radius: 50%;
      border: 1px solid #f0f0f0;
      color: #999;
      font-weight: bold;
      font-size: 0.9em;
      width: 18px;
      height: 18px;
      text-align: center;
      line-height: 18px;
      cursor: pointer;
    }

    .tooltip-icon:hover + .tooltip-text {
      visibility: visible;
      opacity: 1;
    }

    .tooltip-text {
      visibility: hidden;
      width: 280px;
      background-color: #333;
      color: #fff;
      text-align: left;
      border-radius: 5px;
      padding: 8px;
      position: absolute;
      z-index: 1;
      top: -100%;
      right: 0;
      opacity: 0;
      transition: opacity 0.3s;
      font-size: 0.85em;
    }

    .tooltip-text::after {
      content: "";
      position: absolute;
      top: 100%;
      right: 10px;
      border-width: 5px;
      border-style: solid;
      border-color: #333 transparent transparent transparent;
    }
  </style>
</head>
<body>
  <main>
    <section>
      <header>
        <h1>Payment</h1>
      </header>
      <article>
        <form action="/checkout" method="post">
          <label for="amount">Enter Amount (ZAR):</label>
          <div class="input-wrapper">
            <input
              type="number"
              name="amount"
              id="amount"
              min="1"
              step="0.01"
              value="<%= pricePerMilestone %>"
              readonly
              required
            />
            <span class="tooltip-icon">!</span>
            <div class="tooltip-text">
              This amount is calculated from your overall budget divided by the number of milestones you set for the project so that payments may be split equally as per our Terms and Conditions.
            </div>
          </div>
          <br><br>
          <input type="submit" value="Pay Now" />
        </form>
      </article>
    </section>
  </main>
</body>
</html>

